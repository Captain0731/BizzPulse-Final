[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements_production.txt"]

[start]
cmd = "gunicorn app_refactored:app --workers 4 --bind 0.0.0.0:$PORT --timeout 120"

